const APP_NAME = 'Vehicle Repair Shop API';

const tableNames = {
    users: 'users',
};

const userRoles = {
    admin: 'admin',
    customer: 'customer',
};

module.exports = { APP_NAME, tableNames, userRoles };
